field f1 : Int


lockType Accumulator {
  type Int
  invariant(l, v) = [l.f1 |-> ?fv1 && fv1 == v]
  alpha(v): Int = v

  actions = [(Add,Int,duplicable)]

  action Add(v, a)
    requires low(a)
  { v + a }
}