

field f1 : Bool

lockType Flag {
  type Bool
  invariant(l, v) = [l.f1 |-> ?fv1 && fv1 == v]
  alpha(v): Bool = v

  actions = [(Or,Bool,duplicable)]

  action Or(v, a)
    requires low(a)
  { v || a }
}