

field f1 : Bool

lockType Flag {
  type Bool
  invariant(l, v) = [l.f1 |-> ?fv1 && fv1 == v]
  alpha(v): Bool = v

  actions = [(And,Bool,duplicable)]

  action And(v, a)
    requires low(a)
  { v && a }
}